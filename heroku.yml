
build:
  docker:
    worker: Dockerfile
  config:
    NODE_ENV: production
run:
  worker: NODE_OPTIONS="--expose-gc --max-old-space-size=512" node heroku-bot-starter.js --autocleartmp --autoread --keepalive
