const { getMessage } = require('../lib/languages');

var { 
sticker5 
} = require('../lib/sticker');
let handler = async (m, { conn, command, text, usedPrefix }) => {
    // Get user's preferred language
    const user = global.db.data.users[m.sender];
    const chat = global.db.data.chats[m.chat];
    const lang = user?.language || chat?.language || global.language;
 {
  if (!text) throw `🚩 *Example:* ${usedPrefix + command} 🗿`
     await conn.reply(m.chat, wait, m)
     try {
        if (command == 'stikapple') {
        const res = `https://api.betabotz.eu.org/fire/emoji/apple?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikgoogle') {
        const res = `https://api.betabotz.eu.org/fire/emoji/google?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stiksamsung') {
        const res = `https://api.betabotz.eu.org/fire/emoji/samsung?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikmicrosoft') {
        const res = `https://api.betabotz.eu.org/fire/emoji/microsoft?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikwhatsapp') {
        const res = `https://api.betabotz.eu.org/fire/emoji/whatsapp?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stiktwitter') {
        const res = `https://api.betabotz.eu.org/fire/emoji/twitter?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikfacebook') {
        const res = `https://api.betabotz.eu.org/fire/emoji/facebook?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikskype') {
        const res = `https://api.betabotz.eu.org/fire/emoji/skype?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikjoypixels') {
        const res = `https://api.betabotz.eu.org/fire/emoji/joypixels?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikopenmoji') {
        const res = `https://api.betabotz.eu.org/fire/emoji/openmoji?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikemojipedia') {
        const res = `https://api.betabotz.eu.org/fire/emoji/emojipedia?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stiklg') {
        const res = `https://api.betabotz.eu.org/fire/emoji/lg?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikjoypixels') {
        const res = `https://api.betabotz.eu.org/fire/emoji/joypixels?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikhtc') {
        const res = `https://api.betabotz.eu.org/fire/emoji/htc?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikmozilla') {
        const res = `https://api.betabotz.eu.org/fire/emoji/mozilla?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stiksoftbank') {
        const res = `https://api.betabotz.eu.org/fire/emoji/softbank?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikdocomo') {
        const res = `https://api.betabotz.eu.org/fire/emoji/docomo?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
        else if (command == 'stikkddi') {
        const res = `https://api.betabotz.eu.org/fire/emoji/kddi?emoji=${encodeURIComponent(text)}&apikey=${lann}`
            var sticker = await sticker5(res, { packname })
            await conn.sendFile(m.chat, sticker, 'emror.webp', '', m)
        }
    } catch (e) {
        console.log(e)
        throw '*🚩 sticker not di temukan!*'
    }
}

handler.command = handler.help = ['stikapple', 'stikkddi', 'stikgoogle', 'stikdocomo', 'stiksoftbank', 'stikhtc', 'stikmozilla', 'stiklg', 'stikopenmoji', 'stikemojipedia', 'stikjoypixels', 'stikopenmoji', 'stikfacebook', 'stikskype', 'stikwhatsapp', 'stiktwitter', 'stiksamsung', 'stikmicrosoft']
handler.tags = ['sticker'] 
handler.limit = true
}

module.exports = handler
